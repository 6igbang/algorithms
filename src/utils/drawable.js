export default class Drawable {
    constructor (ele) {
        this.ele = ele;
    }

    /**
     * start the chart flow
     */
    run () {}

    /**
     * get the chart binding data
     */
    getData () {}

    /**
     * draw data method
     */
    draw () {}
}